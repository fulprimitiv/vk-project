{
  "name": "backend-api",
  "version": "1.0.0",
  "description": "JSON Server API for VK Project",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "json-server --watch db.json --port 3001 --middlewares middleware.js"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}